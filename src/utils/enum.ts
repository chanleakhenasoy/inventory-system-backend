export enum RoleEnum {
    ADMIN = "admin",
    USER = "user",
    OFFICER = "officer",
    MANAGER = "manager",
}

export enum Status {
  PENDING = 'PENDING',
  RELEASE = 'RELEASE',
  COMPLETED = 'COMPLETED'
}

export enum Currency{
  KH = 'reil',
  USD = 'USD'
}

export enum Payment_Option{
  CARD = 'CARD'
}

export enum Type{
  PURCHASE = 'PURCHASE'
}